sudo: required

language: python

python:
  - '3.9'

before_script:
  - pip install -r requirements.txt

branches:
  only:
    - staging

env: DJANGO_SETTINGS_MODULE="home_credit_api.settings.travis"

services:
  - postresql

script:
  - cd home_credit_api
  - python manage.py test