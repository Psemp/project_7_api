sudo: required

language: python

python:
  - '3.9'

addons:
  postgresql: 13
  apt:
    packages:
      - postgresql-13

before_script:
  - sudo systemctl start postgresql@13-main
  - psql -U postgres -p 5433 -c "CREATE DATABASE home_credit_api;"
  - pip install -r requirements.txt

branches:
  only:
    - staging

env:
  - DJANGO_SETTINGS_MODULE="home_credit_api.settings.travis"

services:
  - postgresql

script:
  - cd home_credit_api
  - python manage.py test
